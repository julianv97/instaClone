import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  image: {
    flex: 1,
  },
  input: {
    margin: 10,
    padding: 10,
    fontSize: 20,
    borderWidth: 1,
    borderColor: '#000',
  },
});

export default styles;
