import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    margin: 10,
    padding: 10,
    fontSize: 20,
    borderWidth: 1,
    borderColor: '#000',
  },
  error: {
    borderColor: 'red',
  },
});

export default styles;
